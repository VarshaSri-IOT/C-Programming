#include <stdio.h>
#include <float.h>

int main()
{
    printf("FLT_MIN = %e\n", FLT_MIN);
    printf("FLT_MAX = %e\n\n", FLT_MAX);

    printf("DBL_MIN = %e\n", DBL_MIN);
    printf("DBL_MAX = %e\n", DBL_MAX);

    return 0;
}